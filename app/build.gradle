plugins {
    id("com.android.application")
}
android {
    namespace = "com.ourlauncher"
    compileSdk = 34
    defaultConfig {
        applicationId = "com.ourlauncher"
        minSdk = 21
        targetSdk = 34
        versionCode = 1
        versionName = "0.1"
        ndk {
            // Matches the prebuilt .so sets bundled under src/main/jniLibs/.
            // Add "x86", "x86_64" here too if you ever need emulator support.
            abiFilters "arm64-v8a", "armeabi-v7a"
        }
    }
    buildTypes {
        release {
            minifyEnabled false
        }
    }
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
}

dependencies {
    implementation("com.google.android.material:material:1.12.0")
    implementation("androidx.appcompat:appcompat:1.7.0")
    implementation("androidx.core:core:1.13.1")
    // Needed to unpack the .tar.xz JRE archives RuntimeManager downloads.
    implementation("org.apache.commons:commons-compress:1.26.1")
    implementation("org.tukaani:xz:1.9")
}
